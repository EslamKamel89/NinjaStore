const TestAlias = () => {
  return (
    <div className="border border-dotted rounded p-2 my-4">
      <strong>Alias test:</strong> imports via <code>@shared</code> work!
    </div>
  );
};

export default TestAlias;
